@model TegoareWeb.Models.Inschrijving

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Inschrijving</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Id_Lid" class="control-label"></label>
                <select asp-for="Id_Lid" class ="form-control" asp-items="ViewBag.Id_Lid"></select>
            </div>
            <div class="form-group">
                <label asp-for="Id_Activiteit" class="control-label"></label>
                <select asp-for="Id_Activiteit" class ="form-control" asp-items="ViewBag.Id_Activiteit"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
